import {   Image   } from 'react-native';

                                  /// ----- Image ------ \\\
        
        

        import Welcome  from './welcomescreen.png';
        export const Export_Welcome = Image.resolveAssetSource(Welcome).uri;


        import Loginscreen  from './loginscreen.png';
        export const Export_Loginscreen = Image.resolveAssetSource(Loginscreen).uri;